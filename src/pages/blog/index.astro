---
import { Image } from "astro:assets";
import Layout from "../../layouts/Layout.astro";
import { getFeed } from "../../lib/posts";
import SideNav from "../../components/SideNav.astro";
import Post from "../../components/Post.astro";
import wolfYell from "../../assets/images/wolf-yell.gif";
import cloverField from "../../assets/images/beautiful-clover-field.jpeg";
import Header from "../../components/Header.astro";

const feed = await getFeed();
---

<Layout>
    <Header backgroundSrc={cloverField.src} decorationSrc={wolfYell.src}  text="Blog"/>
    <div class="flex flex-row gap-10 w-full">
      <SideNav />
      <main class="w-1/2">
        {feed.map((post) => <Post post={post} />)}
      </main>
    </div>
</Layout>
